<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bw.mapper.Dao">
	<select id="zhanshi" resultType="s">
		select * from s
	</select>
	
	<select id="x" resultType="s" parameterType="int">
		select * from s where id=#{id}
	</select>
	
</mapper>